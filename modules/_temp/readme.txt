This dir contains files that are written out from the db to be picked up by other applications (e.g. pick list data).

This readme must exist so that git keeps the _temp directory. Git ignores empty dirs.
